# goit-algo-hw-04

## Домашня робота №4

### Завдання

Python має дві вбудовані функції сортування: sorted і sort. Функції сортування Python використовують Timsort — гібридний алгоритм сортування, що поєднує в собі сортування злиттям і сортування вставками.

Порівняйте три алгоритми сортування: злиттям, вставками та Timsort за часом виконання. Аналіз повинен бути підтверджений емпіричними даними, отриманими шляхом тестування алгоритмів на різних наборах даних. Емпірично перевірте теоретичні оцінки складності алгоритмів, наприклад, сортуванням на великих масивах. Для заміру часу виконання алгоритмів використовуйте модуль timeit.

Покажіть, що поєднання сортування злиттям і сортування вставками робить алгоритм Timsort набагато ефективнішим, і саме з цієї причини програмісти, в більшості випадків, використовують вбудовані в Python алгоритми, а не кодують самі. Зробіть висновки.

#### Необов'язкове завдання

Дано k відсортованих списків цілих чисел. Ваше завдання — об'єднати їх у один відсортований список. При виконанні завдання можете опиратися на алгоритм сортування злиттям з конспекту. Реалізуйте функцію merge_k_lists , яка приймає на вхід список відсортованих списків та повертає відсортований список.

Приклад очікуваного результату:

```python
lists = [[1, 4, 5], [1, 3, 4], [2, 6]]
merged_list = merge_k_lists(lists)
print("Відсортований список:", merged_list)
```

Виведення:

Відсортований список: ```[1, 1, 2, 3, 4, 4, 5, 6]```

### Висновки

Базуючись на загальній характеристиці алгоритмів

Загальна характеристика алгоритмів

| Алгоритм       | Сер. Складність | Стабільність | Пам'ять | Підходить для                             |
|----------------|-----------------|--------------|---------|-------------------------------------------|
| Insertion Sort | O(n²)           | Так          | O(1)    | Малих, майже відсортованих масивів        |
| Merge Sort     | O(n log n)      | Так          | O(n)    | Великих масивів, стабільне сортування     |
| TimSort        | O(n log n)      | Так          | O(n)    | Частково відсортованих або реальних даних |


та наявних результатів виконання сортування:

#### Insertion Sort

— простий в реалізації алгоритм сортування, але підходить лише для малих або майже відсортованих масивів. Його еффективність починає падати вже при середніх об'ємах даних.

#### Merge Sort

— стабільний і надійний алгоритм для великих даних, але потребує додаткової пам’яті.

#### TimSort

— найкращий вибір для реальних задач: адаптивний, стабільний і швидкий
Ефективно обробляє повтори завдяки адаптивності.
Використовується у більшості мов программування за замовчуванням. [LanguageUse.md](data/LanguageUse.md)

При цьому нативна реалізація, яка вже реалізована в стандартній біблотеці має напорядок бульшу швідкість завдяки встроеним оптимізаціям при проботі з процессором та операціями з оперативної пам'ятью
